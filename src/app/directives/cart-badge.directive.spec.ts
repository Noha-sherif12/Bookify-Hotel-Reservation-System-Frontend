import { CartBadgeDirective } from './cart-badge.directive';

describe('CartBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new CartBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
